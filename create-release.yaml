- uses: actions/create-release@latest
    id: create_release
    env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    with:
        tag_name: Latest
        release_name: Latest
        draft: false
        prerelease: true